# Ex04 — Solução

Arquivo:

```text
stack_sentinel/mcp_server/server.py
```

Substitua a função `register_ticket_tool` por:

```python
def register_ticket_tool(server: SimpleMCPServer) -> SimpleMCPServer:
    server.register_tool(
        ToolDefinition(
            name=TICKET_TOOL_NAME,
            description="Busca contexto normalizado de um ticket.",
            input_schema={
                "type": "object",
                "required": ["ticket_id"],
                "properties": {
                    "ticket_id": {"type": "string"},
                },
            },
            handler=tools.fetch_ticket_context,
        )
    )
    return server
```

## Validar

```bash
python run.py test ex04
```

