# Ex09 — Solução

Arquivo:

```text
stack_sentinel/agent/state.py
```

Substitua a função `create_initial_state` por:

```python
def create_initial_state(user_input: str) -> AgentState:
    return {
        "user_input": user_input,
        "intent": None,
        "ticket_id": None,
        "build_id": None,
        "context": None,
        "error": None,
        "final_answer": None,
    }
```

## Validar

```bash
python run.py test ex09
```

## Desafios opcionais — solucao proposta

Para observabilidade simples:

```python
def create_initial_state(user_input: str) -> AgentState:
    return {
        "user_input": user_input,
        "intent": None,
        "ticket_id": None,
        "build_id": None,
        "context": None,
        "error": None,
        "final_answer": None,
        "trace_id": None,
        "steps": [],
    }
```

Se usar `dataclass`, ofereça uma conversão para `dict` antes de passar o state para os nodes testados.
