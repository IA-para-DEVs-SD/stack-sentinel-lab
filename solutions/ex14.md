# Ex14 — Solução

Arquivo:

```text
stack_sentinel/agent/nodes.py
```

Substitua a função `fetch_docs_node` por:

```python
def fetch_docs_node(state: AgentState, mcp_client: MCPClient) -> AgentState:
    resource = mcp_client.read_resource(INCIDENT_RESPONSE_RESOURCE)
    if not resource.get("ok"):
        return update_state(state, context=None, error=resource.get("error", "resource read failed"))

    prompt = mcp_client.get_prompt(
        INCIDENT_TRIAGE_PROMPT,
        {
            "user_question": state.get("user_input", ""),
            "available_context": resource.get("content", ""),
        },
    )
    if not prompt.get("ok"):
        return update_state(state, context=None, error=prompt.get("error", "prompt read failed"))

    return update_state(
        state,
        context={
            "resource": resource,
            "prompt": prompt,
        },
        error=None,
    )
```

## Solução alternativa válida

O resource usado nos testes é `INCIDENT_RESPONSE_RESOURCE`. Um aluno poderia escolher outro resource no projeto real, mas este teste espera esse resource específico.

O ponto importante é que o `context` final tenha exatamente estas chaves:

```python
{
    "resource": resource,
    "prompt": prompt,
}
```

Neste exercício, o node também não deve chamar a FastAPI diretamente; ele deve usar `mcp_client.read_resource` e `mcp_client.get_prompt`, que representam capacidades expostas pelo FastMCP.

## Validar

```bash
python run.py test ex14
```

## Desafios opcionais — solucao proposta

Para escolher resource dinamicamente:

```python
def choose_resource(user_input: str) -> str:
    text = user_input.lower()
    if "severidade" in text:
        return SEVERITY_POLICY_RESOURCE
    if "build" in text:
        return BUILD_FAILURE_PLAYBOOK_RESOURCE
    return INCIDENT_RESPONSE_RESOURCE
```

Para combinar resources:

```python
resources_to_read = [INCIDENT_RESPONSE_RESOURCE, SEVERITY_POLICY_RESOURCE]
contents = [mcp_client.read_resource(uri) for uri in resources_to_read]
available_context = "\n\n".join(item.get("content", "") for item in contents if item.get("ok"))
```
