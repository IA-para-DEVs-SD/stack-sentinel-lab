# Ex06 — Solução

Arquivo:

```text
stack_sentinel/mcp_server/resources.py
```

Resources continuam implementados como funcoes de dominio, mas agora tambem entram no servidor `FastMCP`.

Substitua a função `read_doc_resource` por:

```python
def read_doc_resource(uri: str, client: Optional[MockServiceClient] = None) -> Dict[str, Any]:
    if uri not in RESOURCE_TO_SLUG:
        return {"ok": False, "error": f"unknown resource: {uri}"}

    client = client or MockServiceClient()
    slug = RESOURCE_TO_SLUG[uri]
    response = client.get_doc(slug)

    if not response.get("ok"):
        return {"ok": False, "error": response.get("error", "resource not found")}

    data = response.get("data") or {}
    return {
        "ok": True,
        "uri": uri,
        "title": data.get("title"),
        "content": data.get("content"),
    }
```

## Solução alternativa válida

Também é aceitável chamar diretamente o endpoint FastAPI `GET /docs/{slug}` via `client.get_json(...)`.

```python
def read_doc_resource(uri: str, client: Optional[MockServiceClient] = None) -> Dict[str, Any]:
    if uri not in RESOURCE_TO_SLUG:
        return {"ok": False, "error": f"unknown resource: {uri}"}

    client = client or MockServiceClient()
    slug = RESOURCE_TO_SLUG[uri]
    response = client.get_json(MOCK_ENDPOINTS["docs"].format(slug=slug))

    if not response.get("ok"):
        return {"ok": False, "error": response.get("error", "resource not found")}

    data = response.get("data") or {}
    return {
        "ok": True,
        "uri": uri,
        "title": data.get("title"),
        "content": data.get("content"),
    }
```

Para essa alternativa, ajuste o import:

```python
from stack_sentinel.clients.mock_service_client import MOCK_ENDPOINTS, MockServiceClient
```

## Validar

```bash
python run.py test ex06
```

## Registro no FastMCP

No `fastmcp_server.py`, registre pelo menos o resource principal dentro de uma funcao de registro:

```python
from stack_sentinel.mcp_server import resources
from stack_sentinel.shared.contracts import INCIDENT_RESPONSE_RESOURCE


def register_resources(mcp):
    @mcp.resource(INCIDENT_RESPONSE_RESOURCE)
    def incident_response_resource() -> dict:
        return resources.read_doc_resource(INCIDENT_RESPONSE_RESOURCE)
```

Depois chame `register_resources(mcp)` dentro de `create_fastmcp_server()`.

## Desafios opcionais — solucao proposta

Para registrar todos os resources sem repetir código, capture o `uri` em um factory:

```python
def register_resources(mcp):
    for item in resources.list_doc_resources():
        uri = item["uri"]

        def read_resource(uri=uri):
            return resources.read_doc_resource(uri)

        mcp.resource(uri)(read_resource)
```

Para uma versão textual, crie um adaptador:

```python
def resource_as_text(uri: str) -> str:
    result = resources.read_doc_resource(uri)
    if not result.get("ok"):
        return result.get("error", "resource read failed")
    return f"{result.get('title')}\n\n{result.get('content')}"
```
